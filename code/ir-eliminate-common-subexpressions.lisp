(in-package #:Loopus)

(defun ir-eliminate-common-subexpressions (ir)
  "Returns a copy of IR in which all calls to the same, pure function with
  the same arguments have been replaced by a single call."
  ir)
